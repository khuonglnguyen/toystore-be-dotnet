@{
    IEnumerable<ToyStore.Models.ProductCategory> productCategories = ViewBag.ListProductCategory;
    IEnumerable<ToyStore.Models.Producer> producers = ViewBag.ListProducer;
    IEnumerable<ToyStore.Models.Ages> ages = ViewBag.ListAge;
    IEnumerable<ToyStore.Models.ProductCategoryParent> productCategoryParents = ViewBag.ListParent;
    IEnumerable<ToyStore.Models.Gender> genders = ViewBag.ListGender;
}

@*<script>
    function doIt(item, baseUrl) {
        window.location.href = baseUrl + '?ID=' + item.value + '&page=1';
    }
</script>*@
<!-- //Modal2 -->
<!-- //signup Model -->
<!-- //header-bot -->
<!-- navigation -->
<div class="ban-top">
    <div class="container">
        <div class="agileits-navi_search">
            @*<select id="agileinfo-nav_search" name="productCategoryName" required="" onchange="doIt(this,'@Url.Action("ProductCategory", "Product")')">
                <option value="">Danh mục</option>
                @foreach (var item in productCategories)
                {
                    <option value="@item.ID">@item.Name</option>
                }
            </select>*@
        </div>
        <div class="top_nav_left">
            <nav class="navbar navbar-default">
                <div class="container-fluid">
                    <!-- Brand and toggle get grouped for better mobile display -->
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
                                aria-expanded="false">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                    </div>
                    <!-- Collect the nav links, forms, and other content for toggling -->
                    <div class="collapse navbar-collapse menu--shylock" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav menu__list">
                            <li class="active">
                                <a class="nav-stylehead" href="@Url.Action("index","Home")">
                                    TRANG CHỦ
                                    <span class="sr-only">(current)</span>
                                </a>
                            </li>
                            <li class="">
                                <a class="nav-stylehead" href="@Url.Action("NewProduct","Product")">HÀNG MỚI</a>
                            </li>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle nav-stylehead" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                    SẢN PHẨM
                                    <span class="caret"></span>
                                </a>
                                <ul class="dropdown-menu multi-column columns-3">
                                    <div class="agile_inner_drop_nav_info">
                                        @foreach (var item1 in productCategoryParents)
                                        {
                                            <div class="col-sm-4 multi-gd-img">
                                                <ul class="multi-column-dropdown">
                                                    <li style="font-weight:bold">
                                                        <a onclick="location.href='@Url.Action("ProductCategoryParent","Product",new { ID=item1.ID})'" style="cursor:pointer;text-transform:uppercase;font-size:15px">@item1.Name</a>
                                                    </li>
                                                    @foreach (var item2 in productCategories.Where(x => x.ParentID == item1.ID).OrderBy(x => x.Name))
                                                    {
                                                        <li>
                                                            <a onclick="location.href='@Url.Action("ProductCategory","Product",new { ID=item2.ID})'" style="cursor:pointer">@item2.Name</a>
                                                        </li>
                                                    }
                                                </ul>
                                            </div>
                                        }
                                        <div class="clearfix"></div>
                                    </div>
                                </ul>
                            </li>
                            <li class="dropdown">
                                <a class="nav-stylehead dropdown-toggle" href="#" data-toggle="dropdown">
                                    GIỚI TÍNH
                                    <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu agile_short_dropdown">
                                    @foreach (var item in genders)
                                    {
                                        <li>
                                            <a onclick="location.href='@Url.Action("Gender","Product",new { ID=item.ID})'" style="cursor:pointer">@item.Name</a>
                                        </li>
                                    }
                                </ul>
                            </li>
                            <li class="dropdown">
                                <a class="nav-stylehead dropdown-toggle" href="#" data-toggle="dropdown">
                                    ĐỘ TUỔI
                                    <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu agile_short_dropdown">
                                    @foreach (var item in ages)
                                    {
                                        <li>
                                            <a onclick="location.href='@Url.Action("Ages","Product",new { ID=item.ID})'" style="cursor:pointer">@item.Name</a>
                                        </li>
                                    }
                                </ul>
                            </li>
                            <li class="dropdown">
                                <a class="nav-stylehead dropdown-toggle" href="#" data-toggle="dropdown">
                                    THƯƠNG HIỆU
                                    <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu agile_short_dropdown">
                                    @foreach (var item in producers)
                                    {
                                        <li>
                                            <a onclick="location.href='@Url.Action("Producer","Product",new { ID=item.ID})'" style="cursor:pointer">@item.Name</a>
                                        </li>
                                    }
                                </ul>
                            </li>
                            <li class="">
                                <a class="nav-stylehead" href="@Url.Action("About","Home")">KMD</a>
                            </li>
                            <li class="">
                                <a class="nav-stylehead" href="contact.html">LIÊN HỆ</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>
