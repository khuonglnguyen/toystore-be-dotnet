@model PagedList.IPagedList<ToyStore.Models.Product>
@using PagedList.Mvc;
@{
    ViewBag.Title = "ProductListIsAlmostOver";
    Layout = "~/Views/Layout/AdminLayout.cshtml";
}

<script src="~/Scripts/bootbox.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<script>
    $(document).ready(function () {
        $('.btnImport').off('click').on('click', function () {
            var id = $(this).val();
            $('#modalImport').modal('show');
            $.ajax({
                url: '/ImportProductManage/GetDataForImportProductSingle',
                data: {
                    id: id
                },
                type: 'GET',
                dataType: 'json',
                success: function (response) {
                    if (response.status == true) {
                        var data = response.data;
                        $('#ID').val(data.ID);
                        $('#Name').val(data.Name);
                    }
                    else {
                        bootbox.alert(response.message);
                    }
                },
                error: function (err) {
                    console.log(err);
                }
            });
        });
    });
</script>
<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="@Url.Action("Index")">Trang chủ</a> <i class="fa fa-angle-right"></i></li>
    <li class="breadcrumb-item"><a href="@Url.Action("List")">Sản phẩm</a> <i class="fa fa-angle-right"></i></li>
</ol>

<div class="agile-grids">
    <div class="agile-tables">
        <div class="w3l-table-info">
            <h2>Danh sách sản phẩm</h2>
            @*@using (Html.BeginForm("DeleteMulti", "ProductManage", FormMethod.Post))
                {*@
            <input type="submit" value="Xóa mục đã chọn" class="btn btn-default" id="btnDeleteMulti" />
            <table id="table">
                <thead>
                    <tr>
                        <th>
                            <input type="checkbox" id="checkAll" />
                        </th>
                        <th>
                            Tên sản phẩm
                        </th>
                        <th>
                            Hình ảnh
                        </th>
                        <th>
                            Số Lượng Tồn
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int counter1 = 0;
                    }
                    @foreach (var item in Model)
                    {
                        @Html.Hidden(item.ID.ToString())
                        <tr>
                            <td>
                                <input type="checkbox" class="cbProductCategory" value="@item.ID" name="IDDelete" />
                            </td>
                            <td style="color:brown">
                                <b>@Html.DisplayFor(modelItem => item.Name)</b>
                            </td>
                            <td>
                                <img src="@Url.Content("~/Content/images/" + item.Image1)" width="100" height="100" />
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Quantity)
                            </td>
                            <td>
                                <button class="btn btn-primary btnImport" value="@item.ID">Nhập Thêm</button>
                            </td>
                        </tr>
                        @(counter1++)
                    }
                    @(counter1 = 0)
                </tbody>
            </table>
            @*}*@
            <br />
            <div id='Paging' style="text-align: center">
                @Html.PagedListPager(Model, page => Url.Action("ProductListIsAlmostOver", new { page }))
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modalImport" role="dialog">
    <div class="modal-dialog">

        <!--Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Phiếu Nhập</h4>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm("ImportProductSingle", "ImportProductManage", FormMethod.Post))
                {
                    <div class="form-group">
                        <label for="Name">Tên sản phẩm:</label>
                        <input type="text" class="form-control" name="Name" id="Name">
                        <input type="text" name="page" value="@ViewBag.Page" hidden="hidden" />
                        <input type="hidden" id="ID" name="ProductID"/>
                    </div>
                    <div class="form-group">
                        <label>Giá nhập:</label>
                        <input type="number" class="form-control" name="Price" id="Price">
                    </div>
                    <div class="form-group">
                        <label>Số lượng:</label>
                        <input type="number" class="form-control" name="Quantity" id="Quantity">
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary" id="btnSave">Lưu</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Hủy</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
